{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "cd packages/sample-components && yarn build",
    "start": "yarn build && cd packages/sample-app && yarn start"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
